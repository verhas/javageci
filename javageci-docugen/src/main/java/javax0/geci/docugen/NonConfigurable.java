package javax0.geci.docugen;

/**
 * AbstractSnippeter concrete extensions should implement this interface
 * to signal that they are not configurable and should be invoked to
 * handle a segment even if the segment parameters do not contain any
 * configuration parameter with the mnemonic of the generator.
 */
public interface NonConfigurable {
}

{@import ../pom.jim}
{project |jar|
    {GAV ::javageci-core-annotations:{VERSION}}
    {parent :javageci-parent}
    {name|javageci core annotations}
    {description|Javageci macro library annotations for core code generators}

    {@include ../plugins.jim}

    {dependencies#
        {@for MODULE in (core)=
            {dependency :{GROUPID}:javageci-MODULE:}}
        {@for MODULE in (api,engine)=
            {dependency :org.junit.jupiter:junit-jupiter-MODULE:}}
    }
}
